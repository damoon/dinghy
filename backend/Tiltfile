
k8s_yaml('deployment/kubernetes.yaml')

k8s_resource('backend', port_forwards=['8080', '8081'])

docker_build(
  'backend-image',
  '.',
  dockerfile='deployment/Dockerfile',
  target='workbench',
  live_update=[
    sync('.', '/app'),
    run('cd /app && go build ./cmd/backend'),
  ],
)
