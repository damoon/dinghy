apiVersion: batch/v1
kind: Job
metadata:
  name: backend-tests
spec:
  backoffLimit: 0
  template:
    metadata:
      labels:
        app: backend-tests
    spec:
      containers:
      - name: tests
        image: tests-image
      restartPolicy: Never
